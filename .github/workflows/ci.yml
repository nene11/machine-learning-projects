name: portfolio-ci
on: [push, pull_request]
jobs:
  smoke-tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11"]
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
      - run: python -m pip install --upgrade pip && python -m pip install numpy pandas scikit-learn pytest
      - run: python 07_marketing_campaign_optimization/src/train.py
      - run: python 08_sales_forecasting/src/train.py
      - run: python 09_customer_segmentation/src/train.py
      - run: python 10_end_to_end_mlops/src/train.py
      - name: Run smoke tests without module-name collisions
        run: python -m pytest --import-mode=importlib 07_marketing_campaign_optimization/tests 09_customer_segmentation/tests 10_end_to_end_mlops/tests
