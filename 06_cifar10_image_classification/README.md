# CIFAR-10 Image Classification

**Level:** Advanced–Expert | **Task:** Computer Vision

Dataset: CIFAR-10. Model: convolutional neural network with augmentation, batch normalization, dropout, early stopping and checkpointing.

Skills: image preprocessing, augmentation, CNN architecture, validation design, training controls and error analysis.

Run: `python src/train.py`. GPU is recommended. Metrics are only claimed after actual execution.