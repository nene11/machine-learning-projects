# Credit Risk / Default Prediction

**Level:** Advanced | **Task:** Binary classification + risk scoring

Dataset: UCI German Credit. Approach: calibrated logistic regression scorecard baseline.

Skills: risk modeling, probability calibration, ROC-AUC, Brier score, threshold policy, model governance and leakage control.

Run: `python src/train.py`. Metrics are not claimed until execution is verified.