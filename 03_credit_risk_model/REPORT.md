# Detailed Report — Credit Risk

Objective: estimate credit outcome probabilities and produce a calibrated risk-oriented model.

Pipeline: UCI/OpenML German Credit data → typed preprocessing → stratified split → logistic regression → probability calibration → ROC-AUC and Brier score.

Governance considerations include threshold policy, calibration, sensitive-feature review and human oversight. This is a portfolio model, not a real lending decision system.

**Results: NOT EXECUTED.** Metrics must be generated from an actual run.